# ─────────────────────────────────────────────────────────────────
# GitLab Agentic RAG Chatbot — Dependencies
# 💰 ZERO COST setup: OpenRouter (free models) + local embeddings
# ─────────────────────────────────────────────────────────────────

# ── LLM via OpenRouter ────────────────────────────────────────────
# OpenRouter is OpenAI API-compatible — same SDK, different base_url
openai>=1.30.0               # Used as the HTTP client for OpenRouter

# ── CrewAI (multi-agent orchestration) ───────────────────────────
crewai>=0.55.0
crewai-tools>=0.8.0

# ── Local Embeddings (FREE — runs on your CPU, no API needed) ─────
# Downloads model once (~90MB), then works fully offline
sentence-transformers>=3.0.0
# PyTorch CPU-only (much smaller download than full CUDA version)
torch>=2.2.0
# Tokenizers (dependency of sentence-transformers)
transformers>=4.40.0
huggingface-hub>=0.23.0

# ── Vector Database (local, free) ────────────────────────────────
chromadb>=0.5.0

# ── Document Processing ───────────────────────────────────────────
langchain>=0.2.0
langchain-community>=0.2.0
tiktoken>=0.7.0              # Token counting for chunk size checks

# ── Markdown + Web parsing ────────────────────────────────────────
beautifulsoup4>=4.12.0
markdownify>=0.12.0
PyYAML>=6.0

# ── HTTP / Scraping ───────────────────────────────────────────────
requests>=2.32.0
httpx>=0.27.0

# ── Utilities ─────────────────────────────────────────────────────
python-dotenv>=1.0.0
rich>=13.7.0
tqdm>=4.66.0
pydantic>=2.7.0
loguru>=0.7.0
tenacity>=8.3.0

# ── Dev / Testing ─────────────────────────────────────────────────
pytest>=8.2.0
